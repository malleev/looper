# Документация проекта: Гитарный Лупер (Orange Pi PC)

> **Дата актуализации:** Сентябрь 2026  
> **Платформа:** Orange Pi PC (Allwinner H3, 1GB RAM, Armbian Linux, RT-audio)  
> **Репозиторий:** [malleev/looper](https://github.com/malleev/looper)

---

## 1. Что уже полностью реализовано и работает

### Аудиоядро (Real-Time C++ Engine)
* **Аудио-петля и производительность:**
  * Запись, воспроизведение, наложение (Overdub) без деградации звука (32-bit float).
  * 100% Lock-Free архитектура в аудио-потоке (ноль динамических аллокаций памяти, никаких мьютексов в RT-колбэке ALSA).
  * Бесшовное сшивание лупа (Crossfade 5 мс) — стык петли звучит без щелчков.
  * Реверс (Reverse) и плавное затухание (Fade-out 3 сек).
  * Недеструктивный Undo / Redo (откат и возврат последнего слоя овердаба).
* **Сквозная компенсация задержки (Latency Compensation):**
  * Поддержка двух режимов мониторинга:
    * `DIRECT_ANALOG` (прямой мониторинг звуковой карты, компенсация $K$ сэмплов сдвига наложением).
    * `SOFTWARE` (программный мониторинг через ALSA).
  * Встроенный измеритель реальной аналоговой задержки петли (`looper --calibrate`).
* **Файловая система и сохранение:**
  * Фоновый воркер `WavWorker`: экспорт лупа в чистый 24/32-bit WAV и загрузка без заикания звука.
  * Автоматическое обнаружение подключенной USB-флешки (`/media/looper`).

### Аппаратное управление и индикация
* **6 кнопок (футсвичей):**
  * Обработка через `libgpiod` с наносекундными метками прерываний ядра Linux.
  * Мгновенный отклик и защита от дребезга.
* **Физические светодиоды:**
  * **Статусный RGB-светодиод:** Красный (`REC`), Зеленый (`PLAY`), Желтый (`DUB`), Синий (`STOP`), мигающий зеленый (`FADE`).
  * **Индикатор перегруза (Clip LED):** Отдельный быстрый красный светодиод с моментальной атакой, экспоненциальным спадом и 250 мс удержанием пика.
* **Дисплей 1.77" TFT (ST7735S, SPI):**
  * Подключен по аппаратному SPI (24 МГц).
  * Нативный C++ движок отрисовки `DisplayManager` (~30 FPS, изолированный поток, $<1\%$ CPU).
  * Строгий минималистичный дизайн (Dieter Rams / Teenage Engineering):
    * Крупные контрастные цифры времени (`08.4 s`);
    * 4-долевой геометрический индикатор такта (`[ ][ ][■][ ]`);
    * Плейхед по форме волны и тонкому рельсу;
    * Мгновенный индикатор уровня входа (`IN`) с красной засечкой перегрузки;
    * Статусные бейджи режимов (`REC`, `PLAY`, `DUB`, `REV`, `FADE`, `UNDO`).
* **Тестирование:**
  * 24 из 24 модульных тестов (`looper_tests`) и 4 регрессионных сьюта (`looper_regressions`) проходят на 100%.
  * Постоянные правила udev для доступа без root к GPIO (`/etc/udev/rules.d/99-gpio.rules`) и SPI (`/etc/udev/rules.d/99-spidev.rules`).

---

## 2. Полная карта всех 40 пинов Orange Pi PC

```text
                  ГРЕБЕНКА 40 PIN (ORANGE PI PC)
                     +3.3V [ 1] [ 2] +5V (Питание кодека)
            (I2C SDA) PA12 [ 3] [ 4] +5V (Резерв)
            (I2C SCL) PA11 [ 5] [ 6] GND (RGB LED Земля)
          (RGB Red)    PA6 [ 7] [ 8] PA13 (Резерв GPIO)
           (Clip GND)  GND [ 9] [10] PA14 (Красный Clip LED)
          (RGB Green)  PA1 [11] [12] PD14 (Резерв GPIO)
          (RGB Blue)   PA0 [13] [14] GND (Земля кнопок)
        (Кнопка 1)     PA3 [15] [16] PC4 (Экран: RES / Reset)
    (Экран: VCC 3.3V) +3.3V[17] [18] PC7 (Экран: RS / DC)
    (Экран: SDA MOSI)  PC0 [19] [20] GND (Экран: GND)
       (Резерв GPIO)   PC1 [21] [22] PA2 (Кнопка 2)
    (Экран: SCK CLK)   PC2 [23] [24] PC3 (Экран: CS)
           (I2S GND)   GND [25] [26] PA21 (I2S DOUT -> ЦАП)
     (I2S BCLK)       PA19 [27] [28] PA18 (I2S LRCK -> ЦАП+АЦП)
        (Кнопка 3)     PA7 [29] [30] GND (Резерв GND)
        (Кнопка 4)     PA8 [31] [32] PG8 (Энкодер 1: Фаза A)
        (Кнопка 5)     PA9 [33] [34] GND (Резерв GND)
        (Кнопка 6)    PA10 [35] [36] PG9 (Энкодер 1: Фаза B)
     (I2S DIN <- АЦП) PA20 [37] [38] PG6 (MIDI OUT / UART1_TX)
           (Земля)     GND [39] [40] PG7 (MIDI IN  / UART1_RX)
```

### Сводная таблица подключений:

| Номер пина | Название сигнала | К чему подключено / Назначение |
| :---: | :--- | :--- |
| **Pin 1** | `+3.3V` | **Экран: Pin 8 (LEDA)** — питание подсветки экрана |
| **Pin 2** | `+5V` | **I2S Кодек:** Питание 5V для аналоговой части ЦАП (PCM5102A) и АЦП (PCM1808) |
| **Pin 3** | `PA12` | **Резерв:** Кнопка клика Энкодера 1 (или I2C SDA) |
| **Pin 4** | `+5V` | **Резерв:** Питание 5V |
| **Pin 5** | `PA11` | **Резерв:** Кнопка клика Энкодера 2 (или I2C SCL) |
| **Pin 6** | `GND` | **Статусный RGB LED:** Общая земля (Катод) |
| **Pin 7** | `PA6` | **Статусный RGB LED:** Красный анод (R) |
| **Pin 8** | `PA13` | **Резерв:** Кнопка 7 (или Энкодер 2 Фаза B) |
| **Pin 9** | `GND` | **Клип LED:** Земля (Катод) |
| **Pin 10** | `PA14` | **Клип LED:** Красный анод (R) — вспышка перегрузки |
| **Pin 11** | `PA1` | **Статусный RGB LED:** Зеленый анод (G) |
| **Pin 12** | `PD14` | **Резерв:** Энкодер 2 Фаза A |
| **Pin 13** | `PA0` | **Статусный RGB LED:** Синий анод (B) |
| **Pin 14** | `GND` | **Общая земля кнопок** |
| **Pin 15** | `PA3` | **Кнопка 1:** `ACTION` (Rec / Play / Dub) |
| **Pin 16** | `PC4` | **Экран: Pin 5 (RES)** — аппаратный сброс дисплея |
| **Pin 17** | `+3.3V` | **Экран: Pin 2 (VCC)** — питание логики дисплея ST7735 *(строго 3.3В!)* |
| **Pin 18** | `PC7` | **Экран: Pin 6 (RS / DC)** — переключение Данные/Команда |
| **Pin 19** | `PC0` | **Экран: Pin 4 (SDA)** — линия данных SPI0 MOSI |
| **Pin 20** | `GND` | **Экран: Pin 1 (GND)** — земля дисплея |
| **Pin 21** | `PC1` | **Резерв:** Свободный GPIO (MISO экрана, дисплеем не используется) |
| **Pin 22** | `PA2` | **Кнопка 2:** `REVERSE` (Реверс лупа) |
| **Pin 23** | `PC2` | **Экран: Pin 3 (SCK)** — тактовая линия SPI0 CLK |
| **Pin 24** | `PC3` | **Экран: Pin 7 (CS)** — выбор чипа SPI0 CS |
| **Pin 25** | `GND` | **I2S Кодек:** Земля кодеков PCM5102A / PCM1808 |
| **Pin 26** | `PA21` | **I2S DOUT:** Передача звука из Orange Pi в ЦАП PCM5102A (выход на колонки) |
| **Pin 27** | `PA19` | **I2S BCLK:** Битовый клок шины I2S (общий на ЦАП и АЦП) |
| **Pin 28** | `PA18` | **I2S LRCK:** Выбор левого/правого канала (общий на ЦАП и АЦП) |
| **Pin 29** | `PA7` | **Кнопка 3:** `STOP` (Остановка воспроизведения) |
| **Pin 30** | `GND` | **Резервная земля** |
| **Pin 31** | `PA8` | **Кнопка 4:** `CLEAR` (Очистка лупера) |
| **Pin 32** | `PG8` | **Энкодер 1:** Фаза A (например, регулировка громкости VOL) |
| **Pin 33** | `PA9` | **Кнопка 5:** `UNDO` (Откат/возврат слоя) |
| **Pin 34** | `GND` | **Резервная земля** |
| **Pin 35** | `PA10` | **Кнопка 6:** `FADE` (Плавное затухание 3 сек) |
| **Pin 36** | `PG9` | **Энкодер 1:** Фаза B (например, регулировка громкости VOL) |
| **Pin 37** | `PA20` | **I2S DIN:** Прием оцифрованного звука из АЦП PCM1808 в Orange Pi (вход гитары) |
| **Pin 38** | `PG6` | **MIDI OUT / THRU:** UART1_TX (передача MIDI сообщений через резистор 220 Ом) |
| **Pin 39** | `GND` | **Резервная земля** |
| **Pin 40** | `PG7` | **MIDI IN:** UART1_RX (прием MIDI сообщений через оптопару 6N138) |

---

## 3. Генеральный план развития (Roadmap)

### Этап 1: Подключение I2S аудиокодеков (PCM5102A + PCM1808)
* **Цель:** Избавиться от внешней USB-звуковухи, встроив аудио-тракт прямо в корпус с задержкой $\le 4$ мс.
* **Схема ЦАП PCM5102A (Выход):**
  * `VCC` $\to$ Pin 2 (+5V), `GND` $\to$ Pin 25 (GND).
  * `BCK` $\to$ Pin 27 (PA19), `LCK` $\to$ Pin 28 (PA18), `DIN` $\to$ Pin 26 (PA21).
  * `SCK` $\to$ GND (включение внутреннего PLL автогенератора MCLK).
* **Схема АЦП PCM1808 (Вход):**
  * `VCC` $\to$ Pin 2 (+5V аналог), `VDD` $\to$ +3.3V (цифра), `GND` $\to$ Pin 25.
  * `BCK` $\to$ Pin 27 (PA19), `LRCK` $\to$ Pin 28 (PA18), `DOUT` $\to$ Pin 37 (PA20).
* **Система:**
  * Включение оверлея `sun8i-h3-i2s` в `/boot/armbianEnv.txt`.
  * Калибровка аппаратной задержки с помощью `--calibrate`.

### Этап 2: Вынос управления на внешний микроконтроллер (RP2040)
* **Цель:** Модульная компоновка педали. Все 7 кнопок, энкодеры и светодиоды монтируются на верхней крышке и подключаются к платке **RP2040-Zero**.
* **Связь с Orange Pi:** Всего 1 провод USB (или UART) между крышкой и платой.
* **Бонусы:** Аппаратные автоматы PIO для считывания энкодеров без потери шагов, вход для внешней педали экспрессии через АЦП, адресные RGB-светодиоды WS2812B под каждой кнопкой.

### Этап 3: Экран — выбор и финальная интеграция
* **Текущий SPI 1.77" (160×128):** Полностью рабочий, компактный, готов к установке.
* **Альтернатива (HDMI 3.5" / 5.0" 800×480):** Подключается в штатный разъем HDMI платы. Дает картинку гигантского размера с нулевой нагрузкой на шины ввода-вывода (как в Neural DSP Quad Cortex).
* Выбор определяется размером корпуса педали.

### Этап 4: Потоковое воспроизведение и запись (Streaming Engine)
* **Двухуровневый кэш (RAM + Disk):**
  * Быстрый кольцевой буфер в оперативной памяти (2–4 сек) для 100% бесшовного зацикливания.
  * Фоновый I/O воркер для прямой записи и чтения бесконечных дорожек на USB Flash или eMMC.
* **Плейбек минусовок (Backing Tracks):** Воспроизведение длинных WAV-файлов и игра поверх них.
* Недеструктивные слои овердаба с сохранением на накопитель.

### Этап 5: MIDI (DIN-5 / TRS) и внешний USB-порт
* **Аппаратный MIDI IN/OUT:**
  * Оптопара 6N138 на Pin 40 (RX) и выход на Pin 38 (TX).
  * **MIDI Clock Sync:** Лупер слушает темп драм-машины и автоматически квантует длину лупа по долям.
  * **MIDI CC / PC:** Управление любыми параметрами лупера с внешних педалбордов.
* **Врезной панельный разъем USB-A на корпусе:**
  * Сохранение WAV на флешку на лету по кнопке `SAVE WAV`.
  * Подключение USB-MIDI девайсов и Bluetooth-MIDI свистков (CME WIDI).
